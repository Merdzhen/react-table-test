# react-table-test

Для запуска:
<br>

В папке `server`:
 - `npm i`,
 - создать файл `.env`,
 - создать БД:
   - `npx sequelize db:create`
   - `npx sequelize db:migrate`
   - `npx sequelize-cli db:seed:all`
 - `npm start`
<br>

В папке `client`:
 - `npm i`
 - `npm start`
